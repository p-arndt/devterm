//! DevTerm application entry point.
//!
//! Today this is a placeholder. The M0 walking skeleton replaces it with the winit event
//! loop, a wgpu surface, one ConPTY-backed PowerShell pane, and grid rendering.

fn main() {
    println!(
        "DevTerm {} — walking skeleton pending (see PLAN.md, M0).",
        env!("CARGO_PKG_VERSION")
    );
}
