//! `dt` — control CLI for a running DevTerm instance.
//!
//! Deferred to **M2**. Planned commands talk to the app over a named pipe:
//! `dt open`, `dt split -- npm run dev`, `dt run <profile>`.

fn main() {
    println!("dt: DevTerm control CLI — not yet implemented (see PLAN.md, M2).");
}
